# Basic Usage Example Environment Variables
# This example uses local implementations and requires no external services

# Environment setting
ENVIRONMENT=development
DEBUG=true

# PII Protection (can be disabled for testing)
LOG_PII_PROTECTION=true

# Note: This example uses local/in-memory implementations
# No external API keys or services required